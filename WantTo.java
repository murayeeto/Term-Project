import greenfoot.*;  // (World, Actor, GreenfootImage, Greenfoot and MouseInfo)

/**
 * Write a description of class WantToo here.
 * 
 * @author (your name) 
 * @version (a version number or a date)
 */
public class WantTo extends Actor
{
    /**
     * Act - do whatever the WantTo wants to do. This method is called whenever
     * the 'Act' or 'Run' button gets pressed in the environment.
     */
    public void act()
    {
        
    }
    
    public WantTo(){
        setImage(new GreenfootImage("Want to lead a better life?",32, TitleOptions.getColor(), new Color(0, 0, 0, 0)));
    }
}
